spring:
  application:
    name: config-server
  profiles:
     active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/microservices-chapter/config-repo

server:
  port: 8888

security:
  user:
    name: user
    password: foo

